package edu.umass.cs.iesl.pdf2meta.cli.layoutmodel

import org.scalatest.matchers.ShouldMatchers
import org.scalatest.{BeforeAndAfter, FunSpec}

/**
 * @author <a href="mailto:dev@davidsoergel.com">David Soergel</a>
 * @version $Id$
 */
class RealRectangleTest extends FunSpec with ShouldMatchers with BeforeAndAfter
	{
	describe("A pair of rectangles")
	{
	it("overlaps when fully contained")
	{
	}

	it("overlaps when one corner is contained")
	{
	}

	it("overlaps when one edge is contained")
	{
	}


	it("overlaps when only area is contained")
	{
	}


	it("does not overlap otherwise")
	{
	}
	}
	}
